{
  "name": "viki-spa-website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run compile-scss & live-server",
    "compile-scss": "sass --watch src/styles/main.scss build/main.css",
    "build": "rm -rf build && parcel build ./index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "bootstrap": "^5.3.2",
    "gh-pages": "^6.1.1",
    "live-server": "^1.2.2",
    "parcel": "^2.11.0",
    "sass": "^1.70.0"
  }
}
